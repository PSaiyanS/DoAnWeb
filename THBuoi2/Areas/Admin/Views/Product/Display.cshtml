@model THBuoi2.Models.Product
@{
    ViewData["Title"] = "Product's Detail";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";

}
<!-- Content Header (Page header) -->
<section class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-6">
                <h1>Chi Tiết Sản Phẩm</h1>
            </div>
            <div class="col-sm-6">
                <ol class="breadcrumb float-sm-right">
                    <li class="breadcrumb-item"><a asp-action="Index" asp-controller="Admin"> Home</a></li>
                    <li class="breadcrumb-item active">Chi Tiết Sản Phẩm</li>
                </ol>
            </div>
        </div>
    </div><!-- /.container-fluid -->
</section>

<!-- Main content -->
<section class="content">
    <div class="container-fluid">
       @* <h2>@Model.Name</h2>
        <div>

            @if (!string.IsNullOrEmpty(Model.ImageUrl))
            {
                <img src="@Model.ImageUrl" alt="Product Image" style="max-width:200px;" />

            }
            @if (Model.Images != null && Model.Images.Count > 0)
            {
                @foreach (var imageUrl in Model.Images)
                {
                    <img src="@imageUrl.Url" alt="Product Image" />
                }
            }
            <h3>Price: @Model.Price</h3>
            <p>@Model.Description</p>

        </div>
        <a asp-action="Index">Back to List</a>*@

        

        <!-- content -->
        <section class="py-5">
            <div class="container">
                <div class="row gx-5">
                    <aside class="col-lg-6">
                        <div class="border rounded-4 mb-3 d-flex justify-content-center">
                            <a class="rounded-4">
                                <img style="max-width: 100%; max-height: 100vh; margin: auto;" class="rounded-4 fit" src="@Model.ImageUrl" />
                            </a>
                        </div>
                        <!-- thumbs-wrap.// -->
                        <!-- gallery-wrap .end// -->
                    </aside>
                    <main class="col-lg-6">
                        <div class="ps-lg-3">
                            <h4 class="title text-dark">
                                @Model.Name <br/>  
                                @Model.Category.Name<br/>
                            </h4>
                            
                            <div class="mb-3">
                                <span class="h5">@Model.Price VNĐ</span>
                            </div>

                            <p>
                                @Model.Description
                            </p>                         
                            <a class="btn btn-warning shadow-0" asp-action="Update" asp-route-id="@Model.Id">Edit</a>
                            <a class="btn btn-warning shadow-0" asp-action="Delete" asp-route-id="@Model.Id">Delete</a>
                        </div>
                    </main>
                </div>
            </div>
        </section>
        <!-- content -->
        
		
    </div>
</section>

